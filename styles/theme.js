import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    heading: 'Montserrat',
    body: 'Montserrat',
  },
  color_scheme: {
    red: '#FF3A2C',
  },
});
export default theme;
